int main(int argc, char* argv[])
{
  return 0;
}

@

((FunctionDefinition
  ((ret_type Int)
   (id "main")
   (arg_list
    (ArgList
     (((var_type Int)
       (id "argc")
       (array false))
      ((var_type (Pointer Char))
       (id "argv")
       (array true)))))
   (block
    ((decls ())
     (stmts ()))))))
